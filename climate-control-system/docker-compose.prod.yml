version: "3.9"

services:
  backend:
    image: ghcr.io/your-org/climate-backend:latest
    env_file:
      - .env
    ports:
      - "4000:4000"

  frontend:
    image: ghcr.io/your-org/climate-frontend:latest
    ports:
      - "80:80"
